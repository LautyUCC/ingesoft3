const pool=require('../config/database');
async function teamSales(coverageId){return (await pool.query(`SELECT s.*,t.name team_name FROM coverage_team_sales s JOIN teams t ON t.id=s.team_id WHERE coverage_id=$1`,[coverageId])).rows;}
async function upsertTeamSale(coverageId,teamId,amount,paymentStatus){return (await pool.query(`INSERT INTO coverage_team_sales(coverage_id,team_id,amount,payment_status) VALUES($1,$2,$3,$4) ON CONFLICT(coverage_id,team_id) DO UPDATE SET amount=EXCLUDED.amount,payment_status=EXCLUDED.payment_status,updated_at=NOW() RETURNING *`,[coverageId,teamId,amount,paymentStatus])).rows[0];}
async function removeTeamSale(coverageId,teamId){return (await pool.query('DELETE FROM coverage_team_sales WHERE coverage_id=$1 AND team_id=$2',[coverageId,teamId])).rowCount>0;}
async function coverageFinancials(coverageId){return (await pool.query(`WITH counted AS (SELECT cp.*,NOT EXISTS(SELECT 1 FROM coverage_team_sales ts WHERE ts.coverage_id=cp.coverage_id AND ts.team_id=cp.team_id) OR cp.is_additional_sale ok FROM coverage_players cp WHERE cp.coverage_id=$1), sales AS (SELECT team_id,amount,payment_status FROM coverage_team_sales WHERE coverage_id=$1 UNION ALL SELECT team_id,agreed_price,player_payment_status FROM counted WHERE purchase_type!='none' AND ok) SELECT COALESCE(SUM(amount),0) total,COALESCE(SUM(amount) FILTER(WHERE payment_status='Pagado'),0) collected,COALESCE(SUM(amount) FILTER(WHERE payment_status!='Pagado'),0) pending,COALESCE(SUM(amount) FILTER(WHERE team_id=(SELECT home_team_id FROM coverages WHERE id=$1)),0) home_total,COALESCE(SUM(amount) FILTER(WHERE team_id=(SELECT away_team_id FROM coverages WHERE id=$1)),0) away_total FROM sales`,[coverageId])).rows[0];}
async function dashboardFinancials(){return (await pool.query(`WITH individual AS (SELECT cp.* FROM coverage_players cp WHERE purchase_type!='none' AND (is_additional_sale OR NOT EXISTS(SELECT 1 FROM coverage_team_sales ts WHERE ts.coverage_id=cp.coverage_id AND ts.team_id=cp.team_id))), sales AS (SELECT amount,payment_status,'team' kind FROM coverage_team_sales UNION ALL SELECT agreed_price,player_payment_status,'player' FROM individual) SELECT COALESCE(SUM(amount),0) revenue_total,COALESCE(SUM(amount) FILTER(WHERE payment_status='Pagado'),0) revenue_collected,COALESCE(SUM(amount) FILTER(WHERE payment_status!='Pagado'),0) revenue_pending,COALESCE(SUM(amount) FILTER(WHERE kind='player'),0) individual_sales,COALESCE(SUM(amount) FILTER(WHERE kind='team'),0) team_sales,(SELECT COUNT(*)::int FROM individual) player_buyers,(SELECT COALESCE(SUM(photos_purchased),0)::int FROM individual) photos_sold,(SELECT COUNT(DISTINCT coverage_id)::int FROM (SELECT coverage_id FROM coverage_team_sales WHERE payment_status!='Pagado' UNION ALL SELECT coverage_id FROM individual WHERE player_payment_status!='Pagado') x) coverages_pending FROM sales`)).rows[0];}
module.exports={teamSales,upsertTeamSale,removeTeamSale,coverageFinancials,dashboardFinancials};
